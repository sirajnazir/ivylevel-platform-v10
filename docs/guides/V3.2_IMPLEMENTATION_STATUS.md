# V3.2 Implementation Status

**Last Updated:** 2025-10-23
**Current Phase:** Production Readiness - Database Complete, TypeScript In Progress

---

## ✅ COMPLETED: Database Infrastructure (Migrations 000 + 007)

### Migration 000: Base Schema
- ✅ students table with assessment_mode (interactive/simulated)
- ✅ feature_defs + feature_snapshots (IvyScore system)
- ✅ Seed data: huda-2025, huda-2025-new
- ✅ 16 feature definitions

### Migration 007: v3.2 Production Readiness
- ✅ **Fix #1:** Production facts views (v_awards_facts, v_tests_facts, v_gpa_facts, v_deadlines_facts)
- ✅ **Fix #8:** Temporal UDFs with deterministic tie-breakers (deadline_latest, award_nth, etc.)
- ✅ **Fix #2 (partial):** Outbox table created
- ✅ **Fix #3 (partial):** RLS policies enabled on 4 tables
- ✅ **Fix #4 (partial):** Chips table ready for PII scrubbing
- ✅ **Fix #7 (partial):** EQ profiles + student_coach_eq_tuning tables
- ✅ **Fix #9 (partial):** HGTI tables (growth_events + mv_hgti_scores + ivyscore_history)
- ✅ Agent runs table for budget tracking
- ✅ System events table for monitoring

**Git Commit:** 11e8d9d

---

## 🟡 IN PROGRESS: TypeScript Implementation

### Fix #2: MV Refresh Strategy (Cron Worker)
**Status:** TODO
**Files to create:**
- `services/agent-framework/src/workers/mv-refresher.ts`

**Code:**
```typescript
import { CronJob } from 'cron';
import { pool } from '../db/pool';

const mvRefreshJob = new CronJob('*/5 * * * *', async () => {
  await pool.query(`REFRESH MATERIALIZED VIEW CONCURRENTLY mv_hgti_scores`);
});

mvRefreshJob.start();
```

---

### Fix #3: RLS Session Hygiene
**Status:** TODO
**Files to create/update:**
- `services/agent-framework/src/db/pool.ts` (enhance with RLS helpers)
- `tests/security/rls-negative.test.ts`

**Code:**
```typescript
export async function getClientWithRLS(context: {
  student_id: string;
  coach_id: string;
}): Promise<PoolClient> {
  const client = await pool.connect();
  await client.query(`SET LOCAL app.student_id = $1`, [context.student_id]);
  await client.query(`SET LOCAL app.coach_id = $1`, [context.coach_id]);
  return client;
}
```

---

### Fix #4: Chip Parameter PII Scrubbing
**Status:** TODO
**Files to create:**
- `services/agent-framework/src/chips/chip-creator.ts`
- `tests/chips/scrubbing.test.ts`

**Code:**
```typescript
const PII_PATTERNS = {
  email: /\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b/g,
  phone: /\b\d{3}[-.]?\d{3}[-.]?\d{4}\b/g,
  ssn: /\b\d{3}-\d{2}-\d{4}\b/g,
  dob: /\b\d{4}-\d{2}-\d{2}\b/g,
};

function scrubParams(params: Record<string, any>): Record<string, any> {
  // Redact PII before hashing/saving
}
```

---

### Fix #5: Outbox Exactly-Once Pattern
**Status:** TODO
**Files to create:**
- `services/agent-framework/src/workers/outbox-processor.ts`

**Code:**
```typescript
setInterval(async () => {
  const undelivered = await pool.query(
    `SELECT * FROM outbox WHERE delivered_at IS NULL LIMIT 100`
  );

  for (const event of undelivered.rows) {
    const deliveredKey = `outbox:delivered:${event.id}`;
    const alreadyDelivered = await redis.get(deliveredKey);
    if (alreadyDelivered) continue;

    await eventBus.publish(event.topic, event.payload);
    await redis.setex(deliveredKey, 3600, '1');
    await markDelivered(event.id);
  }
}, 5000);
```

---

### Fix #6: OTel Baggage Propagation
**Status:** TODO
**Files to create/update:**
- `services/agent-framework/src/telemetry/tracer.ts`

**Code:**
```typescript
export async function withSpan<T>(
  operationName: string,
  layer: string,
  fn: (span: any) => Promise<T>,
  context_vars?: { student_id?: string; agent_name?: string; coach_id?: string }
): Promise<T> {
  return tracer.startActiveSpan(operationName, async (span) => {
    span.setAttribute('layer', layer);

    // Attach baggage for cross-service correlation
    if (context_vars) {
      for (const [key, value] of Object.entries(context_vars)) {
        if (value) span.setAttribute(`app.${key}`, value);
      }
    }

    try {
      const result = await fn(span);
      span.setStatus({ code: 1 }); // OK
      return result;
    } catch (error) {
      span.setStatus({ code: 2, message: error.message }); // ERROR
      span.recordException(error);
      throw error;
    } finally {
      span.end();
    }
  });
}
```

---

### Fix #7: EQ Safety Rails
**Status:** TODO
**Files to create:**
- `services/agent-framework/src/cognition/eq-adapter.ts`
- `services/agent-framework/src/ml/embeddings.ts` (stub for now)

**Code:**
```typescript
export class EQAdapter {
  async apply(rawText: string, coachId: number, audience: string): Promise<{ styledText: string; chip: Chip }> {
    const profile = await this.loadProfile(coachId);

    let styled = rawText;

    // 1. Lexical substitutions
    for (const [from, to] of Object.entries(profile.lex_map)) {
      styled = styled.replace(new RegExp(from, 'gi'), to);
    }

    // 2. Embedding similarity guard (≥ 0.85)
    const similarity = await this.computeSimilarity(rawText, styled);
    if (similarity < 0.85) {
      console.warn(`[EQ] Similarity too low (${similarity}), rejecting`);
      styled = rawText; // Fall back
    }

    // 3. Filter banned phrases
    styled = this.filterBanned(styled, GLOBAL_BANNED_PHRASES);

    // 4. Log QA sample
    await this.logQASample(rawText, styled, similarity, traceId);

    const chip = ChipCreator.createEQChip(coachId, profile.tone_vector, audience, traceId, studentId);

    return { styledText: styled, chip };
  }
}
```

---

### Fix #9: IvyScore HGTI Rollout
**Status:** TODO
**Files to create:**
- `services/agent-framework/src/hgti/growth-tracker.ts`
- `services/agent-framework/src/resolvers/readiness.ts`

**Code:**
```typescript
export async function computeIvyScore(
  studentId: string,
  opts: { scoring_version?: number } = {}
): Promise<{ score: number; version: number }> {
  const academicScore = await getAcademicScore(studentId);
  const hgtiScore = await getHGTIScore(studentId);

  const hgtiWeight = getHGTIWeight(opts.scoring_version);
  const academicWeight = 0.40 * (1 - hgtiWeight) / 0.72;

  const ivyScore = academicScore * academicWeight + hgtiScore * hgtiWeight;
  const version = opts.scoring_version || CURRENT_SCORING_VERSION;

  await pool.query(
    `INSERT INTO ivyscore_history (student_id, score, version) VALUES ($1, $2, $3)`,
    [studentId, ivyScore, version]
  );

  return { score: ivyScore, version };
}

function getHGTIWeight(version?: number): number {
  const v = version || CURRENT_SCORING_VERSION;
  if (v === 1) return 0.00; // Baseline
  if (v === 2) return 0.10; // Week 2-3
  if (v === 3) return 0.20; // Week 4
  if (v === 4) return 0.28; // Week 5+ (target)
  return 0.28;
}
```

---

### Fix #10: Ajv Manifest Versioning
**Status:** TODO
**Files to update:**
- `services/agent-framework/src/governance/tool-bus.ts`

**Code:**
```typescript
export interface ToolManifest {
  name: string;
  schema_version: string; // e.g., "1.0.0"
  schema: { type: 'object'; properties: Record<string, any>; required?: string[] };
}

function getValidator(manifest: ToolManifest): any {
  const cacheKey = `${manifest.name}@${manifest.schema_version}`;
  if (validatorCache.has(cacheKey)) {
    return validatorCache.get(cacheKey);
  }
  const validator = ajv.compile(manifest.schema);
  validatorCache.set(cacheKey, validator);
  return validator;
}
```

---

## 📋 NEXT STEPS (Prioritized)

### Phase 1: Core Infrastructure (Today)
1. ✅ Database migrations (DONE)
2. 🟡 Implement Fix #4 (PII scrubbing) - CRITICAL
3. 🟡 Implement Fix #3 (RLS session hygiene) - CRITICAL
4. 🟡 Implement Fix #2 (MV refresher worker) - CRITICAL

### Phase 2: Governance & Safety (Tomorrow)
5. Implement Fix #5 (Outbox processor)
6. Implement Fix #6 (OTel baggage)
7. Implement Fix #7 (EQ safety rails)
8. Implement Fix #10 (Ajv versioning)

### Phase 3: HGTI & Rollout (Day 3)
9. Implement Fix #9 (IvyScore HGTI rollout)
10. Create golden test dataset
11. Run canary validation

### Phase 4: Documentation & Release (Day 4)
12. Update MASTER_PROD_TECH_SPEC.md
13. Update PROD_DB_ARCH.md
14. Update PROD_FEATURE_RELEASE_DETAILS.md
15. Final commit + PR

---

## 🎯 ACCEPTANCE CRITERIA

**Database Infrastructure:** ✅ COMPLETE
- [x] Facts views map to production tables
- [x] Temporal UDFs with stable tie-breakers
- [x] Outbox table created
- [x] RLS policies enabled
- [x] HGTI tables created
- [x] EQ profiles tables created

**TypeScript Implementation:** 🟡 IN PROGRESS (0/10 fixes)
- [ ] Fix #2: MV refresher worker
- [ ] Fix #3: RLS session hygiene
- [ ] Fix #4: PII scrubbing
- [ ] Fix #5: Outbox processor
- [ ] Fix #6: OTel baggage
- [ ] Fix #7: EQ safety rails
- [ ] Fix #9: IvyScore HGTI
- [ ] Fix #10: Ajv versioning

**Documentation:** ⏸️ PENDING
- [ ] MASTER_PROD_TECH_SPEC.md updated
- [ ] PROD_DB_ARCH.md updated
- [ ] PROD_FEATURE_RELEASE_DETAILS.md updated

**Testing:** ⏸️ PENDING
- [ ] Golden dataset created
- [ ] Canary validation (20 students)
- [ ] RLS negative tests passing

---

## 📊 PROGRESS TRACKER

| Category | Items | Completed | % Done |
|----------|-------|-----------|--------|
| Database | 10 | 10 | 100% |
| TypeScript | 10 | 0 | 0% |
| Documentation | 3 | 0 | 0% |
| Testing | 3 | 0 | 0% |
| **TOTAL** | **26** | **10** | **38%** |

---

**Status:** 🟢 Database infrastructure complete and committed (11e8d9d)
**Next:** Implement TypeScript fixes #2-10
**Timeline:** 3-4 days to complete all fixes
