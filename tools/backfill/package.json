{
  "name": "backfill-tools",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "emit-huda": "ts-node emit_huda_observations.ts",
    "emit-colleges": "ts-node emit_college_list.ts",
    "emit-awards": "ts-node emit_award_targets.ts",
    "emit-college-decisions": "ts-node emit_college_decisions_sheet.ts",
    "emit-gameplan": "ts-node emit_gameplan_targets.ts",
    "emit-execution": "ts-node emit_execution_updates.ts",
    "emit-apps": "ts-node emit_applications_subset.ts",
    "emit-all": "npm run emit-gameplan && npm run emit-execution && npm run emit-college-decisions && npm run emit-apps"
  },
  "dependencies": {
    "fast-glob": "^3.3.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.17",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}